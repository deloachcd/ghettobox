# NOTE:
# PROTECT THIS FILE AS IF IT WERE A PRIVATE KEY OR PASSWORD!

# token used in caddyfile for automatic HTTPS with cloudflare/letsencrypt
cloudflare_api_token: ToK3NV4LUe==

# username which owns the samba share in the default config.yml for samba
samba_username: example_username

# password for the above username, in the default config.yml for samba
samba_password: badpasswd

# username for openvpn provider used by transmission
openvpn_username: example_username

# password for openvpn provider used by transmission
openvpn_password: badpasswd

# Username to authenticate to transmission session
transmission_username: example_username

# Password to authenticate to transmission session
transmission_password: badpasswd
