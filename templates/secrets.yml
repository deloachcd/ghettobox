# NOTE:
# PROTECT THIS FILE AS IF IT WERE A PRIVATE KEY OR PASSWORD!
#

# token used in caddyfile for automatic HTTPS with cloudflare/letsencrypt
cloudflare_api_token: ToK3NV4LUe==

# username which owns the samba share, ensure 'example_username' in the
# 'samba_share_config' line of inventory.yml is also replaced with the value
# specified here!
samba_admin_username: example_username

# password to authenticate with the above username as
samba_admin_password: badpasswd
